<h1>{{section.settings.heading}}</h1>

<h3>{{section.settings.question}}</h3>
<h3>{{section.settings.answer}}</h3>

{% schema %}
{
    "name": "customer questions",
    "settings": [
      {
        "type": "text",
        "label": "heading for faq",
        "id": "heading"
      },
      {
        "type": "text",
        "label": "customer questions",
        "id": "question"
      },
      {
        "type": "textarea",
        "label": "answer for faq",
        "id": "answer"
      }
    ]
}
{% endschema %} 